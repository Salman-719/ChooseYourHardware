You are a data extraction agent. Given HTML content from a trusted hardware catalog at {source_url},
return a JSON array of hardware objects with fields:
- name (required)
- url (if present)
- price (string, as seen)
- source (set to the source URL)

Only return valid JSON (no markdown, no explanations).

HTML:
```
{html}
```
